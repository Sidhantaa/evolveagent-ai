{
  "name": "evolveagent-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Native macOS (Tauri) wrapper for the EvolveAgent web app.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
